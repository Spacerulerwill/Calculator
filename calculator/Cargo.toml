[package]
name = "calculator"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.16", features = ["derive"] }
proc_macros = { path = "../proc_macros" }
common = { path = "../common" }